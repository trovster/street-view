{
  "name": "street-view",
  "private": true,
  "scripts": {
    "postinstall": "npm run sync:meteocons",
    "sync:meteocons": "node scripts/sync-meteocons.mjs",
    "test": "node scripts/test.mjs"
  },
  "devDependencies": {
    "@meteocons/svg": "^0.1.0"
  }
}
