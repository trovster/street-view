{
  "name": "street-view",
  "private": true,
  "scripts": {
    "postinstall": "npm run sync:meteocons && npm run fetch:weather",
    "fetch:weather": "node scripts/fetch-weather-data.mjs",
    "sync:meteocons": "node scripts/sync-meteocons.mjs",
    "test": "node scripts/test.mjs"
  },
  "devDependencies": {
    "@meteocons/svg": "^0.1.0"
  }
}
